import 'package:flutter/material.dart';

class AppColors {
  // Official palette specified by user
  static const Color primaryDark = Color(0xFF153243);
  static const Color secondaryDark = Color(0xFF284B63);
  static const Color mutedBorder = Color(0xFFB4B8AB);
  
  // Pure white canvas & cards for seamless merging
  static const Color lightSurface = Colors.white;
  static const Color neutralBackground = Colors.white;

  // Status & accent colors matching MD3 scheme
  static const Color secondaryAccent = Color(0xFF284B63);
  static const Color errorRed = Color(0xFFBA1A1A);
  static const Color errorContainer = Color(0xFFFFDAD6);
  static const Color successGreen = Color(0xFF2E6C40);
  static const Color warningOrange = Color(0xFFD97706);
  static const Color white = Colors.white;
  static const Color transparent = Colors.transparent;
}
